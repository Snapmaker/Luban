#!/usr/bin/env node

var cnc;

if (process.env.NODE_ENV === 'development') {
    cnc = require('../output/server-cli').default;
} else {
    cnc = require('../dist/cnc/server-cli').default;
}

cnc().catch(err => {
    console.error('Error: ', err);
});
