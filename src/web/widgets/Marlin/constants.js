import constants from 'namespace-constants';

module.exports = constants('widgets/Marlin', [
    'MODAL_NONE'
    // 'MODAL_CONTROLLER'
]);

export const TEMPERATURE_MIN = 0;
export const TEMPERATURE_MAX = 300;
