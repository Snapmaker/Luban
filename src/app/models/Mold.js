/**
 * Mold
 *
 * To avoid confusions with old Model class, we use the term "Mold" indicating
 * a mold that represents how a model being presented and processed.
 */
class Mold {
    constructor() {
        this.type = 'Mold';
    }
}

export default Mold;
