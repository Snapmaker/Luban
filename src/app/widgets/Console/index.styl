.widget-content {
    position: relative

    &.hidden {
        display: none;
    }
}

.widget-content-absolute {
    position: absolute;
    top: 42px;
    bottom: 0;
    left: 0;
    right: 0;

    &.hidden {
        display: none;
    }
}

.no-serial-connection {
    padding: 10px;
    color: #333;
    opacity: .65;
}
