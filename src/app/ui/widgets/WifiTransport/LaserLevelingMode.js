const AUTO_MDOE = Symbol('auto mode');
const SEMI_AUTO_MODE = Symbol('semi_auto_mode by MaterialThickness');
const MANUAL_MODE = Symbol('manual mode');

export default {
    AUTO_MDOE,
    SEMI_AUTO_MODE,
    MANUAL_MODE
};
