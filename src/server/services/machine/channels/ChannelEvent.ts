

export enum ChannelEvent {
    // channel connected
    Connected = 'connected',

    // channel disconnected
    Disconnected = 'disconnected',

    /**
     * machine is ready
     * data: {
     *
     * }
     */
    Ready = 'ready',
}
