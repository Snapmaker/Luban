class ScreenHttpController {
    // constructor() {
    //
    // }
    // open(callback = noop, connectionTimeout = 1500){
    //     const { port } = this.options;
    //     this.emit('http:open', { port });
    // }
    //
    // emit(eventName, options) {
    //     const socket = this.connections[id];
    //     socket.emit(eventName, { ...options});
    // }
}
export default ScreenHttpController;
